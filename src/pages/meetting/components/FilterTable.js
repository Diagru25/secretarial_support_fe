import React from "react";
import { Input, DatePicker } from "antd";

export const FilterTable = ({ onFilter }) => {
  return (
    <div
      style={{
        marginTop: "1.5rem",
        marginBottom: "1rem",
      }}
    >
      Filter
    </div>
  );
};
